#pragma once
#include "include/Scene.h"

// vector<std::uint32_t> Scene::draw()
// {
//     // 这里应该实现渲染算法，遍历场景中的物体并将它们渲染到一个像素数组中。
//     // 目前我们先返回一个空的像素数组作为占位符。
//     return vector<std::uint32_t>();
// }